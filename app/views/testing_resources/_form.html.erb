<%= form_with(model: testing_resource) do |form| %>
  <% if testing_resource.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(testing_resource.errors.count, "error") %> prohibited this testing_resource from being saved:</h2>

      <ul>
        <% testing_resource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
